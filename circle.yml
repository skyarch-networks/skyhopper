machine:
  node:
    version: 4.1.0

dependencies:
  cache_directories:
    - frontend/node_modules
    - frontend/typings
    - vendor/bower_components
  override:
    - "true"

database:
  override:
    - "true"
