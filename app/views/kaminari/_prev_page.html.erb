<% disabled = current_page.first? ? 'disabled' : nil %>

<li class="<%= disabled %>">
  <% if disabled %>
    <%= content_tag(:span, raw(t 'views.pagination.previous')) %>
  <% else %>
    <%= link_to raw(t 'views.pagination.previous'), url %>
  <% end %>
</li>
