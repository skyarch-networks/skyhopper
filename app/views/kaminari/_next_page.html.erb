<% disabled = current_page.last? ? 'disabled' : nil %>

<li class="<%= disabled %>">
  <% if disabled %>
    <%= content_tag(:span, raw(t 'views.pagination.next')) %>
  <% else %>
    <%= link_to raw(t 'views.pagination.next'), url %>
  <% end %>
</li>
